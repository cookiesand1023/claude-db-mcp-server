{
  "name": "claude-db-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for Claude CLI database queries",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "claude-db-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "dotenv": "^17.2.3",
    "mysql2": "^3.6.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
